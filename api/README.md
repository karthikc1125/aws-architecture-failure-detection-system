# API
Backend for the system.
- **main.py**: FastAPI entry point.
- **routes.py**: Defines endpoints like `/analyze`.
- **models.py**: Request/Response models.
Running: `uvicorn api.main:app --reload`
