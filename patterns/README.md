# Failure Pattern Engine (Critical)
This directory contains deterministic failure patterns defined in YAML.
These files serve as the "wisdom" of the system, mapping specific conditions to known failure classes and mitigations.
They are NOT generated by LLMs but are hard-coded based on engineering best practices.
